#include "stdafx.h"
#include <iostream>
#include <queue>
#include <string>
using namespace std;
/*
int main() {
	int n, m;
	queue<int> q;
	cin >> n;
	cin >> m;

	for (int i = 1; i <= n; i++) {
		q.push(i);
	}

	for (int i = 1; i < n -1; i++) {
		for(int j = 1; j < m - 1; j++){
			q.push(q.front());
			q.pop();
		}
		cout << q.front();
		q.pop(); 
	}
	cout << q.front();
}*/